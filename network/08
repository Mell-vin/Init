#lgumede: Find the provider of slash16.org/ the previous command from the previous question.

nslookup slash16.org 

#This command finds and gets the the authoritative and non-authoritative DNS servers.

#To get the Nameserver provider of slash16.org , use the following command:

whois slash16.org | grep "Name Server"

#The provider is AWS.